{
  "name": "canvas-api",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start:dev": "wrangler dev --config wrangler.dev.toml",
    "start:prod": "wrangler dev --config wrangler.prod.toml",
    "deploy:dev": "wrangler deploy --config wrangler.dev.toml --minify",
    "deploy:prod": "wrangler deploy --config wrangler.prod.toml --minify"
  },
  "dependencies": {
    "@azure/openai": "^2.0.0",
    "@types/node-fetch": "^2.6.12",
    "hono": "^x.x.x",
    "node-fetch": "^3.3.2",
    "openai": "^4.85.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "wrangler": "^3.101.0"
  }
}
